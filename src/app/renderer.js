import './index.css';
import Jwt from './jwt';
import Home from './home'
console.log('👋 This message is being logged by "renderer.js", included via webpack');

const data = ['', '']
new Home(data);
// new Jwt();
